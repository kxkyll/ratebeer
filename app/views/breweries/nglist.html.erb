<script src="//ajax.googleapis.com/ajax/libs/angularjs/1.2.10/angular.min.js"></script>
<script>
    function BreweriesController($scope, $http) {
        $http.get('breweries.json').success( function(data, status, headers, config) {
            $scope.breweries = data;
        });

        $scope.order = 'name';

        $scope.changeOrder = function (order){
            $scope.order = order;
        }

        $scope.searchText = '';
    }
</script>


<h2>Breweries</h2>

<div ng-app ng-controller="BreweriesController">
  search:  <input ng-model="searchText">

  <table>
    <thead>
    <th> <a ng-click="changeOrder('name')">name</a> </th>
    <th> <a ng-click="changeOrder('year')">style</a> </th>
    
    </thead>
    <tr ng-repeat="brewery in breweries| orderBy:order | filter:searchText">
      <td>{{brewery.name}}</td>
      <td>{{brewery.year}}</td>
    </tr>
  </table>

</div>