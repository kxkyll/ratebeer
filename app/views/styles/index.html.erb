<h1>Listing styles</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Description</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @styles.each do |style| %>
      <tr>
        <td><%= link_to style.name, style %></td>
        <td><%= style.description %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<!--<%= youtube_video @url %> -->

<%= link_to 'New Style', new_style_path if is_admin %>
